#ifndef TYPES_H
#define TYPES_H

#include <stdint.h>  // always use standard fixed-width types

// Optional aliases (only if you really need them)
// Do NOT redefine uint8_t, uint16_t, uint32_t
typedef uint8_t  uint8;
typedef uint16_t uint16;
typedef uint32_t uint32;
typedef uint64_t uint64;

#endif // TYPES_H